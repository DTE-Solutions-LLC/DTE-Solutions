workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == "push"
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH

image: node:20-alpine

stages:
  - build
  - deploy

cache:
  key:
    files:
      - package-lock.json
  paths:
    - node_modules/

build_site:
  stage: build
  script:
    - npm install
    # Bypass lint/TS checks during CI build to isolate webpack/config errors
    - NEXT_TELEMETRY_DISABLED=1 npm run build -- --no-lint
    - mkdir -p FEDPortfolio/
    - cp index.html FEDPortfolio/
    - cp resume.pdf FEDPortfolio/
  artifacts:
    paths:
      - out/
    expire_in: 1 hour

pages:
  stage: deploy
  needs: ["build_site"]
  script:
    - rm -rf public
    - mv out public
    - touch public/.nojekyll
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"

